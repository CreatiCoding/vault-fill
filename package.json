{
  "name": "vault-fill",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @vault-fill/extension dev",
    "build": "yarn workspace @vault-fill/extension build",
    "typecheck": "yarn workspace @vault-fill/extension typecheck"
  },
  "packageManager": "yarn@4.6.0",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
